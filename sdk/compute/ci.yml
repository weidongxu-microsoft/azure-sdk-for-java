# NOTE: Please refer to https://aka.ms/azsdk/engsys/ci-yaml before editing this file.

trigger:
  branches:
    include:
      - main
      - hotfix/*
      - release/*
  paths:
    include:
      - sdk/compute/
    exclude:
      - sdk/compute/pom.xml
      - sdk/compute/azure-resourcemanager-compute/pom.xml

pr:
  branches:
    include:
      - main
      - feature/*
      - hotfix/*
      - release/*
  paths:
    include:
      - sdk/compute/
    exclude:
      - sdk/compute/pom.xml
      - sdk/compute/azure-resourcemanager-compute/pom.xml

parameters:
  - name: release_azureresourcemanagercompute
    displayName: azure-resourcemanager-compute
    type: boolean
    default: false
  - name: release_azureresourcemanagercomputerecommender
    displayName: azure-resourcemanager-compute-recommender
    type: boolean
    default: false
  - name: release_azureresourcemanagercomputedisk
    displayName: azure-resourcemanager-compute-disk
    type: boolean
    default: false
  - name: release_azureresourcemanagercomputecompute
    displayName: azure-resourcemanager-compute-compute
    type: boolean
    default: false

extends:
  template: ../../eng/pipelines/templates/stages/archetype-sdk-client.yml
  parameters:
    ServiceDirectory: compute
    Artifacts:
      - name: azure-resourcemanager-compute
        groupId: com.azure.resourcemanager
        safeName: azureresourcemanagercompute
        releaseInBatch: ${{ parameters.release_azureresourcemanagercompute }}
      - name: azure-resourcemanager-compute-recommender
        groupId: com.azure.resourcemanager
        safeName: azureresourcemanagercomputerecommender
        releaseInBatch: ${{ parameters.release_azureresourcemanagercomputerecommender }}
      - name: azure-resourcemanager-compute-disk
        groupId: com.azure.resourcemanager
        safeName: azureresourcemanagercomputedisk
        releaseInBatch: ${{ parameters.release_azureresourcemanagercomputedisk }}
      - name: azure-resourcemanager-compute-compute
        groupId: com.azure.resourcemanager
        safeName: azureresourcemanagercomputecompute
        releaseInBatch: ${{ parameters.release_azureresourcemanagercomputecompute }}
